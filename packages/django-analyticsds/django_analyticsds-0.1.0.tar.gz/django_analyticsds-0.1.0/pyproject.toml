[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "django-analyticsds"
version = "0.1.0"
description = "One of the my demiansoft apps"
authors = [{name = "Hyungjin Kim", email = "hj3415@gmail.com"}]
license = {file = "LICENSE"}
readme = "README.md"
classifiers = ["License :: OSI Approved :: MIT License"]
dependencies = [
    "Django >= 5.0.3",
]

[project.urls]
Home = "https://www.demiansoft.com"

[tool.flit.sdist]
exclude = [
    "django_analyticsds/_data/",
    "django_analyticsds/_mytester/",
    "django_analyticsds/_static/",
    "django_analyticsds/media/",
    "django_analyticsds/db.sqlite3",
    "django_analyticsds/manage.py",
]
