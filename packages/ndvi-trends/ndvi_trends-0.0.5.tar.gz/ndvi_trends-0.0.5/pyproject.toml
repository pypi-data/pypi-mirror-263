[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "ndvi_trends"
version = "0.0.5"
readme = "README.md"
dependencies = []

[tool.setuptools]
packages = [
	"ndvi_trends"
]