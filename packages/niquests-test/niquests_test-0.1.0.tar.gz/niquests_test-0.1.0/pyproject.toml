[tool.poetry]
name = "niquests-test"
version = "0.1.0"
description = ""
authors = ["ABGalkin <ABGalkin@alfabank.ru>"]
readme = "README.md"
packages = [{include = "niquests_test", from = "src"}]

[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
