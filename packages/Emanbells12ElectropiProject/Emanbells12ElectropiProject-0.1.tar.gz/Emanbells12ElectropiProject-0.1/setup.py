# setup.py

from setuptools import setup, find_packages

setup(
    name='Emanbells12ElectropiProject',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        'pandas',
        'numpy'
    ],
)
