import {{ base_module }}
import time
from .schema import {{ upper_camel_case(trigger) }}Input, {{ upper_camel_case(trigger) }}Output, Input, Output, Component
# Custom imports below


class {{ upper_camel_case(trigger) }}({{ base_module }}.Trigger):

    def __init__(self):
        super(self.__class__, self).__init__(
                name="{{ trigger }}",
                description=Component.DESCRIPTION,
                input={{ upper_camel_case(trigger) }}Input(),
                output={{ upper_camel_case(trigger) }}Output())

    def run(self, params={}):
        # START INPUT BINDING - DO NOT REMOVE - ANY INPUTS BELOW WILL UPDATE WITH YOUR PLUGIN SPEC AFTER REGENERATION
        # END INPUT BINDING - DO NOT REMOVE

        while True:
            # TODO: Implement trigger functionality
            self.send({})
            time.sleep(5)
