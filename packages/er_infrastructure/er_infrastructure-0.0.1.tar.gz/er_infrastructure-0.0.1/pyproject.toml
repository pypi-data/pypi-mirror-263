[tool.poetry]
name = "er_infrastructure"
version = "0.0.1"
description = "A stub for the EntityRisk PROVEN Platform infrastructure package"
authors = ["EntityRisk, Inc <info@entityrisk.com>"]
readme = "README.md"
packages = [{include = "er_stubs"}]

[tool.poetry.dependencies]
python = "^3.9"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
