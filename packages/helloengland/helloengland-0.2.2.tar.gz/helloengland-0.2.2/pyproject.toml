[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "helloengland"
description = "hello world!"
authors = [{name = "Janghoo Lee", email = "dlwkdgn1@naver.com"}]
version = "0.2.2"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "click"
]

[project.scripts]
sayhelloto = "helloengland.cli.sayhelloto:main"

[project.optional-dependencies]
dev = [
    "pytest"
]
