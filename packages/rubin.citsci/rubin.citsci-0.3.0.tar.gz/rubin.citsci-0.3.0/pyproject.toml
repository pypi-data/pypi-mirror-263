[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "rubin.citsci"
version = "0.3.0"
readme = "README.md"
dependencies = [
    "panoptes_client",
    'google.cloud.storage',
    'requests',
]