[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "userinterface"
authors = [
    {name = "Ruud van der Ham", email = "rt.van.der.ham@gmail.com"}
]
description = "userinteface description"
version = "0.0.8"
readme = "README.md"
requires-python = ">=3.7"
dependencies = [
]
