[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "pyarrow"
version = "0.0.dev1"
authors = [{ name = "Hashboard" }]
requires-python = ">=3.8"
classifiers = ["Programming Language :: Python :: 3"]
dependencies = []

[tool.setuptools]
package-dir = { 'pyarrow' = 'src/' }
